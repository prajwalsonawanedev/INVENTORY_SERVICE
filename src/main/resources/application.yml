server:
  port: 8081

spring:
  application:
    name: Inventory-Service

  datasource:
    url: jdbc:mysql://localhost:3306/prajwalpoc?createDatabaseIfNotExist=true
    username: root
    password: "Prajwal@123"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # Can also be "create" (for dev) or "validate"
    show-sql: true       # Logs SQL queries to console
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
